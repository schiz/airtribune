- Собираем итоговый стилевой файл на основе style.scss, в коммиты его не включаем.

- В этой директории (/styles) в идеале должны остаться только некомпилированные SCSS-файлы, весь CSS уходит после компиляции в директорию /css

